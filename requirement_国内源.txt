# ============================================================
#  StoryBoardStudio 运行依赖 —— 国内镜像版
#
#  【怎么用】把本文件重命名为 requirements.txt，覆盖原文件即可。
#            启动脚本固定读 requirements.txt，不需要动任何脚本。
#
#  【怎么回退】用完想回官方源，把 requirements.txt 还原即可：
#              git checkout requirements.txt
#
#  本文件与 requirements.txt 的依赖完全一致，只多出下面那行 --index-url。
#  其他可选镜像（换着用，改 --index-url 那行的地址即可）：
#    阿里云   https://mirrors.aliyun.com/pypi/simple/
#    腾讯云   https://mirrors.cloud.tencent.com/pypi/simple/
#    中科大   https://pypi.mirrors.ustc.edu.cn/simple/
# ============================================================
--index-url https://pypi.tuna.tsinghua.edu.cn/simple

gradio>=4.20.0
pandas>=2.0.0
openpyxl>=3.1.2
Pillow>=10.0.0
fastapi
uvicorn
