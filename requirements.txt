# ============================================================
#  StoryBoardStudio 运行依赖
#
#  默认走官方 PyPI 源（https://pypi.org/simple），无需任何额外配置。
#  如遇网络问题想临时指定别的源，在命令后加 -i 即可，不必改本文件：
#    pip install -r requirements.txt -i <某个 PyPI 镜像地址>
#
#  国内网络需要长期加速时：把同目录的 `requirement_国内源.txt`
#  重命名为 requirements.txt 覆盖本文件即可（依赖完全一致，只多一行镜像）。
#  想改回官方源：git checkout requirements.txt
# ============================================================

gradio>=4.20.0
pandas>=2.0.0
openpyxl>=3.1.2
Pillow>=10.0.0
fastapi
uvicorn
